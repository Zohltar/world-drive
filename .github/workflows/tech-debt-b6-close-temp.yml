name: Sync tech debt B6 completion

on:
  push:
    branches:
      - docs/b6-close
    paths:
      - 'qa/patch-tech-debt-b6-close-temp.mjs'
      - '.github/workflows/tech-debt-b6-close-temp.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node qa/patch-tech-debt-b6-close-temp.mjs
      - name: Commit plan
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/WORLD_DRIVE_TECH_DEBT_PLAN.md
          git diff --cached --quiet || git commit -m 'Docs: close B6 wheelspin ownership cleanup'
          git push

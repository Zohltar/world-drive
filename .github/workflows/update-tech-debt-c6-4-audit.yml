name: Update Tech Debt C6.4 Audit

on:
  push:
    branches:
      - docs/c6-4-audit-plan
    paths:
      - tools/c6-4-audit.trigger
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/update-tech-debt-c6-4-audit.mjs
      - shell: bash
        run: |
          if git diff --quiet -- docs/WORLD_DRIVE_TECH_DEBT_PLAN.md; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/WORLD_DRIVE_TECH_DEBT_PLAN.md
          git commit -m 'Docs: record C6.4 audit boundary'
          git pull --rebase origin docs/c6-4-audit-plan
          git push origin HEAD:docs/c6-4-audit-plan

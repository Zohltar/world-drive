name: Sync tech debt R23 B4

on:
  push:
    branches:
      - audit/momentum-b4
    paths:
      - 'qa/patch-tech-debt-r23-b4-temp.mjs'
      - '.github/workflows/tech-debt-r23-b4-temp.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node qa/patch-tech-debt-r23-b4-temp.mjs
      - name: Commit plan sync
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add docs/WORLD_DRIVE_TECH_DEBT_PLAN.md
          git diff --cached --quiet || git commit -m 'Docs: sync R23 and B4 ownership audit'
          git push

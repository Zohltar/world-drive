// Foret P9.33 startup readiness wrapper. The P9 scenery renderer remains the
// visual implementation; P9.33 only prevents gameplay from starting before a
// useful initial forest ring exists.
export {createSceneryRenderer} from './scenery/scenery-renderer-p933.js';
